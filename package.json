{
  "name": "triangle-packing-toolkit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prepare-sites.js",
    "preview": "vite preview",
    "test": "node --test",
    "benchmark": "node benchmarks/run.js",
    "check": "npm test && npm run build"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
